const container = document.querySelector('#main');

// Create the grid square
const square = document.createElement('div');
square.classList.add('grid-square');

// Make 40 of them per row and column

container.appendChild(square);
